package com.chargealert.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Centralized spacing so the redesigned screen reads as one deliberate
 * composition (generous, consistent gaps) instead of default per-component
 * padding scattered across files.
 */
object Spacing {
    val screenHorizontal = 24.dp
    val screenTop = 24.dp
    val screenBottom = 40.dp
    val sectionGap = 36.dp
    val rowVertical = 16.dp
    val withinSection = 4.dp
}
